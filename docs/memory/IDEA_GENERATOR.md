# Idea Generator

Use this file to keep compact, high-ROI next-task ideas without long brainstorming.

## Rule

- Ideas must be small enough to become one bounded task.
- Prefer ideas that improve truthfulness, throughput, or promotion readiness.
- Do not store essays here.

## Current top ideas

1. ~~`service` — add one compact parser promotion KPI strip to operator/API surfaces.~~ DONE (GET /api/v1/parsers/promotion-kpi)
2. ~~`service` — surface lane/task status from swarm board in a read-only developer panel.~~ DONE (GET /api/v1/swarm/status)
3. ~~`coordinator` — auto-promote worker handoff into backlog/task seeds when confidence is high.~~ DONE (self-driving rules)
4. ~~`winline` — convert validated structured path evidence into a hard accept/reject promotion gate.~~ DONE (readiness() method)
5. ~~`melbet` — isolate the exact bootstrap dependency boundary with one tighter runtime probe.~~ DONE (runtime_probe expanded)
6. ~~`betboom` — prove whether Sporthub/runtime emptiness is transport, parse, or payload shape.~~ DONE (root cause: PARSE)
7. ~~`ligastavok` — make browser-assisted bootstrap evidence diffable across runs.~~ DONE (bootstrap fixture)
8. ~~`service` — add compact token-cost hints to swarm docs and handoff policy.~~ DONE (HANDOFF_TEMPLATE.md)

## New ideas

1. ~~`betboom` — fix block splitting logic for flattened cards or accept diagnostic-only.~~ PROMOTED (`betboom-fix-blocksplit`)
2. ~~`melbet` — test actual bootstrap against live melbet.ru to confirm cloudflare barrier.~~ PROMOTED (`melbet-test-bootstrap`)
3. ~~`coordinator` — generate next wave from current blockers and idea pool.~~ DONE (`coordinator-generate-wave`)
4. ~~`service` — add parser readiness to API health endpoint.~~ PROMOTED (`service-health-readiness`)
5. `service` — add nightly parser KPI diagnostics as a promotion gate
6. `service` — build a fixture-driven discovery-to-parser harness
7. `execution` — expose approval/rejection clarity in operator execution center

## Drop rule

- Delete ideas once they become task-board entries or prove low-value.
