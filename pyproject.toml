[project]
name = "ghost_imperium"
version = "1.0.0"
description = "Professional sports arbitrage scanner"
requires-python = ">=3.10"

# pytest configuration lives in pytest.ini to avoid split sources of truth.

[tool.ruff]
line-length = 120
target-version = "py310"
